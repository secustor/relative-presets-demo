{
  "name": "relative-presets-demo-app",
  "version": "0.0.0",
  "private": true,
  "description": "Deliberately outdated dependencies, so Renovate has something to raise pull requests for.",
  "dependencies": {
    "@aws-sdk/client-s3": "3.400.0",
    "lodash": "4.17.20"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
